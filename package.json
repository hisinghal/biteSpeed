{
  "name": "bite",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prestart": "npm run clean &&  npm run -s build",
    "start": "node dist/index.js",
    "clean": "rimraf dist",
    "build": " babel src -s -D -d dist"
  },
  "author": "himanshu",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.2",
    "express": "^4.18.2",
    "mysql2": "^3.5.1"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0"
  }
}
